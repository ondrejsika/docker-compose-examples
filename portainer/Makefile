use-ports:
	echo COMPOSE_FILE=docker-compose.yml:docker-compose.ports.yml > .env

use-traefik-http:
	echo COMPOSE_FILE=docker-compose.yml:docker-compose.traefik-http.yml > .env

use-traefik-https:
	echo COMPOSE_FILE=docker-compose.yml:docker-compose.traefik-https.yml > .env
